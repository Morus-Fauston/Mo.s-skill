---
status: accepted
---

# 使用共享维护契约，同时保持 Skill 独立

本仓库将工作上下文、证据状态和确认门集中在 `skills/_shared/references/` 维护，以减少九个本地 Skill 的语义漂移；每个 `SKILL.md` 仍保留触发、边界和完成条件，因此单独安装时不依赖共享目录。同一 Agent、同一上下文连续调用多个 Skill 时不要求填写表单；只有跨 Agent、跨会话或长期留档时才按需摘要。选择这一方式是为了在统一语义、工作效率和独立分发之间折中。