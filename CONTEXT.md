# Mo's Skill 工作流

本上下文定义本仓库用于编排本地路由 Skill、上游执行器和可复核记录的核心术语。

## Language

**工作项**：
一次需要被识别、执行、等待确认或跨 Agent/会话继续的有边界工作。
_Avoid_: 任务包、流程实例

**入口 Skill**：
识别当前问题类型、建立工作项并决定执行器或确认门的本地 Skill。
_Avoid_: 总控 Skill、自动调度器

**执行器**：
完成代码审查、研究、原型、UI、浏览器验证或项目测试等具体工作的上游 Skill 或项目工具。
_Avoid_: 下游 Skill

**记录 Skill**：
把决定、证据、验收或发布结果保存为可复核产物并给出停止条件的本地 Skill。
_Avoid_: 报告生成器

**工作项状态**：
工作项在提出、执行、等待用户、阻塞、完成或被拒绝之间的生命周期状态。
_Avoid_: 验证结果、阶段结论

**验证项状态**：
单个验证目标的通过、失败、待人工、阻塞或不适用结果。
_Avoid_: 工作项状态、阶段结论

**阶段结论**：
阶段、版本或里程碑根据全部必需验收项得出的通过、部分通过、失败或阻塞结论。
_Avoid_: 验证项状态

**确认门**：
在产品取舍、原型体验、真实环境、正式写入或 Git 动作前保留给用户的明确决定点。
_Avoid_: 自动转交、例行询问

**共享契约**：
维护多个本地 Skill 时统一工作上下文、证据状态和确认门语义的参考文件；同一上下文连续调用 Skill 不需要填写表单，跨 Agent/会话时才按需摘要。
_Avoid_: 运行时依赖、独立 Skill